#!/data/data/com.termux/files/usr/bin/bash
# Руслан — быстрое обновление (git pull)
cd ~/Руслан && git fetch --depth=1 origin master && git reset --hard origin/master && pip install -e . 2>&1 | tail -3
echo ""
echo -e "\033[0;32m✔ Руслан обновлён!\033[0m"
